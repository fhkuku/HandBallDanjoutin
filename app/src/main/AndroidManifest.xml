<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fernando.handballdanjoutin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.photos" />
        <activity android:name=".Activity.videos" />
        <activity
            android:name=".Activity.club"
            android:theme="@style/AppThemeWithBar" />
        <activity android:name=".Activity.contact" />
        <activity android:name=".Activity.show_info"></activity>
    </application>

</manifest>